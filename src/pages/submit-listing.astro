---
import Layout from "../layouts/Layout.astro";

const created = Astro.url.searchParams.get("created");
const owner = Astro.locals.owner;
---

<Layout title="Post a Listing | Property App 2026" description="Owners can submit property listings directly into the marketplace.">
  <section class="page-head card">
    <div>
      <span class="pill">Owner flow</span>
      <h1 class="section-title">Post a listing</h1>
      <p class="section-copy">
        Listings are now tied to real owner accounts. Every new listing starts with a 7-day free trial, after which it
        can be converted into a paid or promoted placement.
      </p>
    </div>
    <div class="actions">
      <a class="ghost" href="/owner/dashboard/">Owner dashboard</a>
      <form method="POST" action="/api/auth/logout">
        <button class="ghost" type="submit">Log out</button>
      </form>
    </div>
  </section>

  {created && (
    <div class="notice card">
      Listing created successfully. New slug: <code>{created}</code>. Add photos from the <a href="/owner/dashboard/">owner dashboard</a>.
    </div>
  )}

  <div class="pricing card">
    <strong>Monetization path</strong>
    <ul>
      <li>Free: every listing is visible for 7 days.</li>
      <li>Paid: keep a listing live after the free-trial window.</li>
      <li>Promoted: push the listing above standard inventory.</li>
    </ul>
  </div>

  <form class="form-shell card" method="POST" action="/api/listings">
    <div class="grid two">
      <label>Listing title<input name="title" required placeholder="Modern 3BR condo in Thu Duc" /></label>
      <label>Intent<select name="intent"><option value="buy">Buy</option><option value="rent">Rent</option></select></label>
    </div>

    <div class="grid three">
      <label>City<input name="city" required placeholder="Ho Chi Minh City" /></label>
      <label>District<input name="district" required placeholder="Thu Duc City" /></label>
      <label>Ward<input name="ward" required placeholder="An Khanh" /></label>
    </div>

    <div class="grid four">
      <label>Property type<select name="propertyType"><option>Condo</option><option>Villa</option><option>Townhouse</option><option>Apartment</option><option>House</option><option>Land</option></select></label>
      <label>Price label<input name="priceLabel" required placeholder="9.5B VND" /></label>
      <label>Numeric price<input name="numericPrice" type="number" min="0" step="0.01" required placeholder="9.5" /></label>
      <label>Price unit<select name="priceUnit"><option value="VND">VND</option><option value="VND / month">VND / month</option></select></label>
    </div>

    <div class="grid three">
      <label>Beds<input name="beds" type="number" min="0" required value="2" /></label>
      <label>Baths<input name="baths" type="number" min="0" required value="2" /></label>
      <label>Area (m2)<input name="area" type="number" min="1" required value="90" /></label>
    </div>

    <label>Short summary<textarea name="summary" rows="3" required placeholder="High-floor condo with strong owner response and easy metro access."></textarea></label>
    <label>Full description<textarea name="description" rows="6" required placeholder="Describe the home, location, condition, and why it stands out."></textarea></label>

    <div class="location-context card-inset">
      <strong>Location context</strong>
      <p>
        This matters more than generic marketing copy. Add the commute, landmarks, and neighborhood cues a buyer would
        actually use to decide whether to enquire.
      </p>
      <label>Neighborhood headline<textarea name="neighborhoodHeadline" rows="3" placeholder="A calmer family pocket in Thu Duc with strong access to international schools and riverfront services."></textarea></label>
      <label>Commute and access<textarea name="commuteNotes" rows="3" placeholder="About 15 to 20 minutes to Thu Thiem, 25 minutes to District 1 outside peak traffic."></textarea></label>
      <div class="grid two">
        <label>Nearby landmarks or amenities<input name="nearbyPlaces" placeholder="International school, Metro stop, Retail podium" /></label>
        <label>Trust signals<input name="trustSignals" placeholder="Low-rise pocket, Verified owner, Strong rental fallback" /></label>
      </div>
      <div class="grid three">
        <label>Latitude<input name="lat" type="number" step="0.000001" placeholder="10.7826" /></label>
        <label>Longitude<input name="lng" type="number" step="0.000001" placeholder="106.7297" /></label>
        <label>Map precision label<input name="locationPrecisionLabel" placeholder="Approximate area in Thu Thiem" /></label>
      </div>
    </div>

    <div class="grid three">
      <label>Owner name<input name="ownerName" required placeholder="Nguyen Anh" value={owner?.full_name ?? ""} /></label>
      <label>Owner email<input name="ownerEmail" type="email" required placeholder="owner@example.com" value={owner?.email ?? ""} /></label>
      <label>Owner phone<input name="ownerPhone" required placeholder="+84 ..." value={owner?.phone ?? ""} /></label>
    </div>

    <button type="submit">Publish listing</button>
  </form>

  <style is:inline>
    .page-head,
    .form-shell,
    .notice,
    .pricing {
      padding: 1.4rem;
      margin-bottom: 1rem;
    }
    .pricing ul {
      margin: 0.6rem 0 0;
      padding-left: 1.1rem;
      color: var(--muted);
      line-height: 1.7;
    }
    .page-head {
      display: flex;
      justify-content: space-between;
      gap: 1rem;
      align-items: flex-start;
    }
    .actions {
      display: flex;
      gap: 0.75rem;
      flex-wrap: wrap;
    }
    .ghost,
    button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.95rem 1.2rem;
      border-radius: 999px;
      text-decoration: none;
      font-weight: 700;
    }
    .ghost {
      border: 1px solid var(--line);
      background: rgba(255,255,255,0.72);
    }
    .form-shell {
      display: grid;
      gap: 1rem;
    }
    .grid {
      display: grid;
      gap: 0.9rem;
    }
    .card-inset {
      display: grid;
      gap: 0.9rem;
      padding: 1.1rem;
      border-radius: 24px;
      background: rgba(248, 250, 252, 0.88);
      border: 1px solid var(--line);
    }
    .card-inset strong {
      font-size: 1.05rem;
      letter-spacing: -0.02em;
    }
    .card-inset p {
      margin: 0;
      color: var(--muted);
      line-height: 1.7;
    }
    .grid.two { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .grid.three { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .grid.four { grid-template-columns: repeat(4, minmax(0, 1fr)); }
    label {
      display: block;
      color: var(--muted);
      font-size: 0.92rem;
      font-weight: 600;
    }
    input, select, textarea {
      width: 100%;
      margin-top: 0.4rem;
      padding: 0.95rem 1rem;
      border-radius: 18px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,0.8);
      font: inherit;
    }
    button {
      border: none;
      background: linear-gradient(135deg, var(--accent), var(--accent-strong));
      color: white;
      cursor: pointer;
    }
    code { font-family: monospace; }
    @media (max-width: 960px) {
      .grid.two, .grid.three, .grid.four, .page-head { grid-template-columns: 1fr; display:grid; }
    }
  </style>
</Layout>
