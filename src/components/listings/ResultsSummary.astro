---
export interface Props {
  total: number;
  countries: number;
  cities: number;
  forRent: number;
  hasFilters: boolean;
  allCities: string[];
  selectedIntent: string;
  t?: (value: string) => string;
}

const { total, countries, cities, forRent, hasFilters, allCities, selectedIntent, t = (value: string) => value } = Astro.props;
---

<section class="results-summary card" id="results">
  <div class="summary-main">
    <strong>{total} {t("listings")}</strong>
    <p>
      {t("Across")} {countries} {t("countries")} · {cities} {t("cities")} · {forRent} {t("rentals live")}
      {hasFilters ? ` · ${t("filtered results")}` : ` · ${t("full inventory")}`}
    </p>
    <div class="summary-pills">
      <span>{selectedIntent === "rent" ? t("Rental search") : t("Purchase search")}</span>
      <span>{hasFilters ? t("Custom filter set") : t("Full live inventory")}</span>
    </div>
  </div>
  <div class="summary-links">
    <span>{t("Popular city routes")}</span>
    {allCities.slice(0, 3).map((city) => <a href={`/listings/?intent=${selectedIntent}&city=${encodeURIComponent(city)}`}>{city}</a>)}
  </div>
</section>

<style is:inline>
  .results-summary {
    display: flex;
    justify-content: space-between;
    gap: var(--space-m);
    align-items: flex-start;
    flex-wrap: wrap;
    margin-bottom: var(--space-m);
    padding: var(--space-l);
    background: var(--surface-overlay-strong);
    box-shadow: var(--shadow-soft);
  }

  .results-summary strong {
    display: block;
    font-size: var(--text-4xl);
    letter-spacing: -0.08em;
  }

  .results-summary p,
  .summary-links span {
    color: var(--muted);
    margin-top: var(--space-xs);
    line-height: 1.72;
    font-weight: 700;
  }

  .summary-main {
    display: grid;
    gap: var(--space-xs);
  }

  .summary-pills {
    display: flex;
    gap: var(--space-s);
    flex-wrap: wrap;
  }

  .summary-pills span {
    display: inline-flex;
    align-items: center;
    min-height: calc(var(--space-l) * 0.95);
    padding: calc(var(--space-2xs) * 0.9) var(--space-s);
    border-radius: var(--radius-full);
    background: var(--surface-elevated);
    border: 1px solid var(--line);
    color: var(--muted-strong);
    font-size: var(--text-xs);
    font-weight: 700;
  }

  .summary-links {
    display: flex;
    justify-content: space-between;
    gap: var(--space-s);
    align-items: center;
    flex-wrap: wrap;
  }

  .summary-links a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: calc(var(--space-xl) + var(--space-xs));
    border-radius: var(--radius-full);
    padding: var(--space-s) var(--space-m);
    font-weight: 700;
    text-decoration: none;
    font: inherit;
    border: 1px solid var(--line);
    background: var(--surface-elevated-strong);
    color: var(--text);
  }

  @media (max-width: 760px) {
    .results-summary,
    .summary-links {
      display: grid;
    }
  }
</style>
