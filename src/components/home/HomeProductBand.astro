<section class="product-band card">
  <div class="band-copy">
    <span class="section-kicker">Mobile app path</span>
    <h2>Start as a serious web marketplace. Let native come later.</h2>
    <p>
      PWA support is already the right base. It gives you installability and repeat-use behavior now, while the core
      marketplace mechanics get proven first.
    </p>
  </div>
  <div class="band-panel">
    <div class="panel-window">
      <p class="panel-label">Saved search</p>
      <strong>2-bed condo under 9B VND</strong>
      <span>Thu Duc · District 7 · Binh Thanh</span>
    </div>
    <div class="panel-window muted">
      <p class="panel-label">Owner inbox</p>
      <strong>3 new enquiries</strong>
      <span>2 require follow-up today</span>
    </div>
  </div>
</section>

<style is:inline>
  .product-band {
    margin-top: 2.35rem;
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(0, 0.92fr);
    gap: var(--space-m);
    padding: var(--space-l);
    border-radius: calc(var(--radius-lg) * 2);
    background: linear-gradient(180deg, color-mix(in srgb, var(--brand) 92%, black), var(--brand))
    color: var(--surface-canvas);
  }

  .section-kicker,
  .panel-label {
    display: inline-flex;
    color: var(--surface-panel-muted);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .band-copy {
    display: grid;
    gap: var(--space-s);
    align-content: start;
  }

  .band-copy h2 {
    margin: 0;
    font-size: var(--text-display);
    line-height: 0.9;
    letter-spacing: -0.1em;
    color: var(--surface-canvas);
  }

  .band-copy p,
  .panel-window span {
    color: var(--surface-panel-muted);
    line-height: 1.75;
  }

  .band-panel {
    display: grid;
    gap: 0.9rem;
    align-content: center;
  }

  .panel-window {
    display: grid;
    gap: 0.28rem;
    padding: var(--space-m);
    border-radius: calc(var(--radius-lg) * 1.35);
    background: color-mix(in srgb, var(--surface-canvas) 10%, transparent);
    border: 1px solid var(--border-strong);
  }

  .panel-window strong {
    font-size: var(--text-base);
    letter-spacing: -0.04em;
  }

  .panel-window.muted {
    background: color-mix(in srgb, var(--surface-canvas) 8%, transparent);
  }

  @media (max-width: 1080px) {
    .product-band {
      grid-template-columns: 1fr;
    }
  }
</style>
